provider "azurerm" {
  features {}

  # The following block is optional and can be used for authentication
  # Uncomment and fill in the details if needed
  # client_id       = var.client_id
  # client_secret   = var.client_secret
  # tenant_id       = var.tenant_id
  # subscription_id = var.subscription_id
}